namespace CanaryLib;

enum ContentFormat:byte { 
  RawMessage = 0,
  Flatbuffers = 1,
}

table Header {
  checksum:uint32 = 0;
  size:uint16 = 0;
  format:ContentFormat = RawMessage;
}

root_type Header;